# 🎲 布局权重系统使用指南

## 功能概述

布局权重系统允许为每个布局模板分配权重值，容器在选择布局时会根据权重进行随机选择，权重越高的布局被选中的概率越大。

## 💡 核心特性

### 1. 权重机制
- **权重值**：支持正数（包括小数），权重为0则永不选中
- **概率计算**：某布局被选中概率 = 布局权重 ÷ 所有布局权重总和
- **智能降级**：如果布局没有权重字段，自动降级为等概率选择

### 2. 界面支持
- **权重显示**：布局编辑器中显示每个布局的权重值
- **可视化编辑**：点击权重值即可直接编辑
- **实时更新**：权重修改后立即生效，支持API同步

## 📋 使用说明

### 布局编辑器操作

1. **查看权重**：
   - 在布局模板列表中，每个布局会显示其权重值
   - 权重以绿色徽章形式显示：`权重: 5`

2. **编辑权重**：
   - 点击权重值打开编辑对话框
   - 输入新的权重值（支持小数）
   - 确认后自动保存到服务器

3. **新建布局**：
   - 新建的布局默认权重为 1
   - 可以立即编辑权重值

### 配置文件格式

布局JSON文件中的权重字段：

```json
{
  "layout_1": {
    "name": "基础布局",
    "description": "简单的布局配置",
    "positions": [...],
    "weight": 5
  }
}
```

## 🎯 权重策略建议

### 按复杂度分层
```
空布局:     权重 0.1   (基本不被选中)
简单布局:   权重 3-5   (1-3个物品)
标准布局:   权重 5-7   (4-6个物品)
复杂布局:   权重 2-4   (7+个物品)
特殊布局:   权重 0.5-1 (稀有布局)
```

### 按容器类型优化
```
低级容器:   偏向简单布局 (高权重)
高级容器:   平衡所有布局类型
特殊容器:   突出特定布局
```

## 🔧 技术实现

### 代码位置
- **类型定义**: `src/types/index.ts` - Layout接口
- **选择算法**: `src/utils/gameLogic.ts` - selectLayout函数
- **界面编辑**: `public/layout-editor.html` - 权重编辑功能

### 关键函数
```typescript
// 加权随机选择
function weightedRandomSelect<T extends { weight: number }>(options: T[]): T

// 布局选择 (支持权重)
function selectLayout(layouts: Layout[]): Layout
```

## 🧪 测试工具

### 1. 权重分布测试
- 访问: `/test-layout-weights.html`
- 功能: 验证权重分布是否符合预期
- 用法: 选择测试次数，查看统计结果

### 2. 完整功能验证
- 访问: `/test-weight-verification.html`
- 功能: 全面测试权重系统状态
- 用法: 运行完整测试，检查各项指标

## 📊 监控和调试

### 权重效果检查
```bash
# 检查权重覆盖率
jq '[.[] | select(.weight != null)] | length' json/layouts.json

# 查看权重分布
grep -o '"weight": [0-9.]*' json/layouts.json | sort | uniq -c
```

### 游戏内验证
- 开箱多次观察布局分布
- 使用浏览器控制台查看选择日志
- 对比期望概率与实际结果

## 🚀 最佳实践

1. **权重设置**：
   - 使用相对权重而不是绝对值
   - 避免权重差距过大（建议不超过10倍）
   - 为新布局设置合理的默认权重

2. **测试验证**：
   - 修改权重后进行分布测试
   - 使用足够的样本量（建议1000+次）
   - 定期检查权重效果

3. **用户体验**：
   - 确保高质量布局有适当权重
   - 避免无聊布局权重过高
   - 保持一定的随机性和惊喜感

## 🔄 版本历史

- **v1.0**: 基础权重系统实现
- **v1.1**: 添加界面编辑支持
- **v1.2**: 完善测试工具和文档

---

*此功能已完全集成到Delta Force开箱模拟器中，享受更智能的布局选择体验！* 🎮
